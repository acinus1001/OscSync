이 모듈은 백엔드/프론트엔드 구분하지 않고 공통으로 사용되는 기능을 모아두는 모듈입니다.
컴파일 환경이 다르기 때문에 모듈 생성 시 최소한 타겟을 jvm+js, 가능하다면 jvm+js+wasm 으로 설정하여야 합니다.
코드는 commonMain에 생성해 주세요.

아래 모듈들은 프로젝트 내 어떤 다른 모듈도 종속성을 가지지 않는 것을 원칙으로 하지만, types 내부의 모듈의 경우 직렬화 편의성을 위해 date/serialization
모듈의 종속성을 가집니다.