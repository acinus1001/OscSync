[CREATE TABLE IF NOT EXISTS webhook_notify_send_log (seq BIGSERIAL PRIMARY KEY, domain_type VARCHAR(20) NOT NULL, channel_id BIGINT NOT NULL, guild_id BIGINT NULL, phase VARCHAR(10) NOT NULL, "exception" TEXT NULL, send_date TIMESTAMP NOT NULL, send_data_info VARCHAR(100) NULL, send_data_seq BIGINT NULL)], [CREATE TABLE IF NOT EXISTS webhook_subscribe_channel (guild_id BIGINT NULL, webhook_url VARCHAR(500) NOT NULL, webhook_id BIGINT NOT NULL, registered_user_id BIGINT NOT NULL, created_at TIMESTAMP NOT NULL, revoked_at TIMESTAMP NULL, domain_type VARCHAR(20), channel_id BIGINT, CONSTRAINT pk_webhook_subscribe_channel PRIMARY KEY (domain_type, channel_id))], [CREATE TABLE IF NOT EXISTS vrc_chess_game_log (id BIGSERIAL PRIMARY KEY, user_ip_hash INT NOT NULL, game_id BIGINT NOT NULL, fen VARCHAR(100) NOT NULL, "move" VARCHAR(10) NOT NULL, pgn VARCHAR(10) NOT NULL, turn_type VARCHAR(5) NOT NULL, is_bot_move BOOLEAN NOT NULL, created_at TIMESTAMP NOT NULL, undo_at TIMESTAMP NULL)], [CREATE TABLE IF NOT EXISTS vrc_chess_game (id BIGSERIAL PRIMARY KEY, white_user_ip_hash INT NOT NULL, black_user_ip_hash INT NOT NULL, bot_elo INT NULL, fen VARCHAR(100) NOT NULL, created_at TIMESTAMP NOT NULL, fen_updated_at TIMESTAMP NOT NULL, ended_at TIMESTAMP NULL)]