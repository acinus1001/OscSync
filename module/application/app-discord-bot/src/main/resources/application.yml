spring:
  profiles:
    include:
      - ai
      - member
      - discord
      - database
      - redis
      - dbg # discord-bot-guilds
server:
  port: 0

management:
  server:
    port: 9090

logging:
  level:
    root: info
    com.zaxxer.hikari.pool.HikariPool: info
    exposed: debug

dev:
  kuro9:
    jwt:
      key: test123
    webhook:
      url: ${DISCORD_ERROR_WEBHOOK}
    music-connecter:
      host: ${MUSIC_CONNECTER_HOST}