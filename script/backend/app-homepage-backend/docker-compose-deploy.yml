services:
  app:
    container_name: app-homepage-backend
    image: app-homepage-backend
    env_file:
      - ${ENV_FILE_LOC}
    networks:
      - database
    ports:
      - "60080:8080"

networks:
  database:
    external: true
